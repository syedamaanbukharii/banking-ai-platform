# Template only — do NOT commit real secrets. Create via your secrets manager
# (e.g. Sealed Secrets, External Secrets, Vault) in a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: banking-ai-secrets
  namespace: banking-ai
type: Opaque
stringData:
  SECURITY_JWT_SECRET: "replace-with-32+char-random-secret"
  DATABASE_URL: "postgresql+asyncpg://user:pass@postgres:5432/banking"
  REDIS_URL: "redis://redis:6379/0"
  GROQ_API_KEY: "replace-me"
  OIDC_CLIENT_SECRET: "replace-me"
